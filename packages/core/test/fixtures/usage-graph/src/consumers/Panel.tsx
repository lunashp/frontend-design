import { Button } from '../ui/Button/Button';

/** Direct named import of Button (#2 of the two by-name importers). */
export function Panel() {
  return (
    <section>
      <Button>Close</Button>
    </section>
  );
}
