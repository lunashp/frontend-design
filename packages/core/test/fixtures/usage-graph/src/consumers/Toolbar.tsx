import { Button } from '../ui/Button/Button';

/** Direct named import of Button (#1 of the two by-name importers). */
export function Toolbar() {
  return (
    <div>
      <Button>Save</Button>
    </div>
  );
}
