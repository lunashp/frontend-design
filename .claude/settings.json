{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -d node_modules ]; then corepack enable >/dev/null 2>&1 || true; pnpm install --frozen-lockfile || pnpm install; pnpm exec playwright install chromium >/dev/null 2>&1 || true; fi"
          }
        ]
      }
    ]
  }
}
